apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-https
  namespace: argocd
spec:
  parentRefs:
    - name: external-http
      namespace: argocd
  hostnames:
    - 34.111.216.1.nip.io
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80

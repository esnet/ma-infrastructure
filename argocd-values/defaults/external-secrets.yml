installCRDs: false
nameOverride: "external-secrets"

crds:
  createAggregateRoles: true
  annotations:
    meta.helm.sh/release-name: external-secrets
    meta.helm.sh/release-namespace: external-secrets
  # This prevents kubectl from adding last-applied-configuration
  labels:
    app.kubernetes.io/managed-by: Helm

## TODO: this need to change per project
serviceAccount:
  name: external-secrets-sa
  annotations:
    iam.gke.io/gcp-service-account: external-secrets-gsa@ma-infrastructure-474617.iam.gserviceaccount.com
